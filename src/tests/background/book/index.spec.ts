import { clearBook } from "@/background/book";

describe("background/book", () => {
  beforeEach(() => {
    clearBook();
  });

  it("openBook", async () => {
    // FIXME
  });

  it("saveBook", async () => {
    // FIXME
  });

  it("updateBookMove", async () => {
    // FIXME
  });

  it("updateBookMoveOrder", async () => {
    // FIXME
  });

  it("importBookMoves", async () => {
    // FIXME
  });
});
